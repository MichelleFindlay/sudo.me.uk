<?php
// ---- sudo.me.uk local configuration ----
// Copy this file to config.php and fill in real values there. config.php is
// gitignored — never commit real secrets to this repo. index.php requires
// config.php to exist.

// current release version — shown in the top-bar badge and compared against
// the latest GitHub release to flag when this deploy is out of date
$VERSION = '1.6.5';
